{
  "name": "ville",
  "version": "1.0.0",
  "description": "Procedural city generator based on user-painted canvas.",
  "scripts": {
    "build": "uglifycss css/style.css --output style.min.css && minify src/*.js --out-file script.min.js --mangle.keepClassName"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexwgraves/ville.git"
  },
  "author": "Alex Graves",
  "license": "ISC",
  "devDependencies": {
    "babel-minify": "^0.5.0",
    "uglifycss": "0.0.29"
  }
}
