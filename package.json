{
  "name": "ville",
  "version": "1.0.0",
  "description": "A user input-based procedural city generator",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist/ && parcel build index.html",
    "dev": "rm -rf dist/ && parcel watch index.html"
  },
  "keywords": [],
  "author": "Alex Graves",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.10.3"
  },
  "dependencies": {
    "src": "^1.1.2"
  }
}
